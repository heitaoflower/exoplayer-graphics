apply from: '../../constants.gradle'
apply plugin: 'com.android.library'

android {
    compileSdkVersion project.ext.compileSdkVersion
    buildToolsVersion project.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion project.ext.minSdkVersion
        targetSdkVersion project.ext.targetSdkVersion

        externalNativeBuild {
            cmake {
                abiFilters 'armeabi-v7a'
            }
        }
    }
    buildTypes {

    }

    externalNativeBuild {
        cmake {
            path 'CMakeLists.txt'
        }
    }
    sourceSets { main { jni.srcDirs = ['src/main/c', 'src/main/jni', 'src/main/c/model', 'src/main/c/sensor', 'src/main/c/context'] } }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')

    compile 'com.google.android.exoplayer:exoplayer-core:' + exoplayLibraryVersion
    compile 'com.google.android.exoplayer:exoplayer-hls:' + exoplayLibraryVersion
}
